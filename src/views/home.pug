extends base.pug
include mixins/video.pug

block headerNav 
  nav 
        ul 
          li 
            a(href="/videos/upload") Upload Video

block content
  each video in videos
    +video(video)
  else
    li Nothing was found.